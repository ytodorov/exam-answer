<ul class="panelbar">
    <li class="k-state-active">
        <span class="k-link k-state-selected">Question @Model?.Id</span>
        <div>
            <p>
                @Html.Raw(Model.H1OpenTag)
                The sales management team of Universal Containers has noticed that opportunities are taking longer to close.
                @Html.Raw(Model.H1CloseTag)
            </p>
            <p>
                @Html.Raw(Model.H2OpenTag)
                Historically, it has taken 30 days for a new opportunity to be moved to closed/won. 
                @Html.Raw(Model.H2CloseTag)
            </p>
            <p>
                Recently, this time period has increased to 45 days.
            </p>
            <p>
                What analytics tool can the sales management team leverage to help determine the cause? (Choose 2 answers)
            </p>
        </div>
    </li>
    <li class="k-state-active">
        Answers
        <div>
            <p>
                <ul class="fieldlist">
                    <li class="correct">
                        <input type="checkbox" id="@(Html.GetNextId())" class="k-checkbox">
                        <label class="k-checkbox-label" for="@(Html.GetNextId(true))">
                            Dashboard of opportunity stage duration
                        </label>
                    </li>
                    <li>
                        <input type="checkbox" id="@(Html.GetNextId())" class="k-checkbox">
                        <label class="k-checkbox-label" for="@(Html.GetNextId(true))">
                            Report on campaign return on investment (ROI)
                        </label>
                    </li>
                    <li>
                        <input type="checkbox" id="@(Html.GetNextId())" class="k-checkbox">
                        <label class="k-checkbox-label" for="@(Html.GetNextId(true))">
                            Dashboard of Month-over- month trend of lead conversions
                        </label>
                    </li>
                    <li class="correct">
                        <input type="checkbox" id="@(Html.GetNextId())" class="k-checkbox">
                        <label class="k-checkbox-label" for="@(Html.GetNextId(true))">
                            Report on the discount approval time for quotes
                        </label>
                    </li>
                </ul>
            </p>
        </div>
    </li>
</ul>