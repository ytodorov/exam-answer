<ul class="panelbar">
    <li class="k-state-active">
        <span class="k-link k-state-selected">Question @Model?.Id</span>
        <div>
            <p>
                @Html.Raw(Model.H1OpenTag)
                A salesperson at Northern Trail Outfitters (NTO) cannot view a contact's information from social profiles.
                @Html.Raw(Model.H1CloseTag)
            </p>
            <p>
                @Html.Raw(Model.H2OpenTag)
                NTO has Social Accounts and Contacts turned on in its account.
                @Html.Raw(Model.H2CloseTag)
            </p>
            <p>
                Why is the salesperson unable to access the information?
            </p>
        </div>
    </li>
    <li class="k-state-active">
        Answers
        <div>
            <p>
                <ul class="fieldlist">
                    <li>
                        <input type="radio" name="group" id="@(Html.GetNextId())" class="k-radio">
                        <label class="k-radio-label" for="@(Html.GetNextId(true))">
                            The fields configured by the administrator on the contact page layout are missing.
                        </label>
                    </li>
                    <li>
                        <input type="radio" name="group" id="@(Html.GetNextId())" class="k-radio">
                        <label class="k-radio-label" for="@(Html.GetNextId(true))">
                            The link to the Facebook profile is NOT configured with the administrator password to access detailed information.
                        </label>
                    </li>
                    <li>
                        <input type="radio" name="group" id="@(Html.GetNextId())" class="k-radio">
                        <label class="k-radio-label" for="@(Html.GetNextId(true))">
                            The information shown is based on the sales representative's social connection with the contact.
                        </label>
                    </li>
                    <li>
                        <input type="radio" name="group" id="@(Html.GetNextId())" class="k-radio">
                        <label class="k-radio-label" for="@(Html.GetNextId(true))">
                            The administrator must install and APP Exchange package to access public profile information for its users.
                        </label>
                    </li>
                </ul>
            </p>
        </div>
    </li>
</ul>