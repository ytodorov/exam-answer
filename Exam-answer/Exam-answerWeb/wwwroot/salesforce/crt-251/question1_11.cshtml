<ul class="panelbar">
    <li class="k-state-active">
        <span class="k-link k-state-selected">Question @Model?.Id</span>
        <div>
            <p>
                @Html.Raw(Model.H1OpenTag)
                Universal Containers wants to capture business sector information on a lead and display the information on the account and contact once the lead has been converted.
                @Html.Raw(Model.H1CloseTag)
            </p>
            <p>
                @Html.Raw(Model.H2OpenTag)
                How can these requirements be met? 
                @Html.Raw(Model.H2CloseTag)
            </p>
        </div>
    </li>
    <li class="k-state-active">
        Answers
        <div>
            <p>
                <ul class="fieldlist">
                    <li>
                        <input type="radio" name="group" id="@(Html.GetNextId())" class="k-radio">
                        <label class="k-radio-label" for="@(Html.GetNextId(true))">
                            Create a custom field on the Lead, Account, and Contact objects and configure mapping of these two field for conversion. Use a trigger to update the Contact field with the Account value.
                        </label>
                    </li>
                    <li>
                        <input type="radio" name="group" id="@(Html.GetNextId())" class="k-radio">
                        <label class="k-radio-label" for="@(Html.GetNextId(true))">
                            Create a custom field on the Lead and Account objects. Create a custom formula field on the Contact object to pull the value from the Account object.
                        </label>
                    </li>
                    <li class="correct">
                        <input type="radio" name="group" id="@(Html.GetNextId())" class="k-radio">
                        <label class="k-radio-label" for="@(Html.GetNextId(true))">
                            Create a custom field on the Lead and Account objects and configure mapping of these two fields for conversion. Create a custom formula field on the Contact object to pull value form the Account object.
                        </label>
                    </li>
                    <li>
                        <input type="radio" name="group" id="@(Html.GetNextId())" class="k-radio">
                        <label class="k-radio-label" for="@(Html.GetNextId(true))">
                            Create a custom field on the Lead and Account objects and configure mapping of these two fields for conversion. Create a custom formula field on the Account object to pull value form the Contact object.
                        </label>
                    </li>
                </ul>
            </p>
        </div>
    </li>
</ul>