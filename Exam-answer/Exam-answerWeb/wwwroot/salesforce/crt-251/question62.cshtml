<ul class="panelbar">
    <li class="k-state-active">
        <span class="k-link k-state-selected">Question @Model?.Id</span>
        <div>
            <p>
                @Html.Raw(Model.H1OpenTag)
                Northern Trail Outfitters' (NTO) consumer business has grown to more than 500,000 contacts.
                @Html.Raw(Model.H1CloseTag)
            </p>
            <p>
                @Html.Raw(Model.H2OpenTag)
                NTO stores all individual consumer contacts under a single account called "Consumer." Mass updates are no longer completed within the defined maintenance timeframe and an increased number of errors are being reported.                
                @Html.Raw(Model.H2CloseTag)
            </p>
            <p>
                Which two actions should be recommended to improve performance?
            </p>
            <p>
                Choose 2 answers
            </p>
        </div>
    </li>
    <li class="k-state-active">
        Answers
        <div>
            <p>
                <ul class="fieldlist">
                    <li>
                        <input type="checkbox" id="@(Html.GetNextId())" class="k-checkbox">
                        <label class="k-checkbox-label" for="@(Html.GetNextId(true))">
                            Enable person accounts and migrate the contact data.
                        </label>
                    </li>
                    <li>
                        <input type="checkbox" id="@(Html.GetNextId())" class="k-checkbox">
                        <label class="k-checkbox-label" for="@(Html.GetNextId(true))">
                            Add an index to the account field on the contact object.
                        </label>
                    </li>
                    <li>
                        <input type="checkbox" id="@(Html.GetNextId())" class="k-checkbox">
                        <label class="k-checkbox-label" for="@(Html.GetNextId(true))">
                            Remove the account assignment for all contacts.
                        </label>
                    </li>
                    <li>
                        <input type="checkbox" id="@(Html.GetNextId())" class="k-checkbox">
                        <label class="k-checkbox-label" for="@(Html.GetNextId(true))">
                            Ensure that no single account has more than 10,000 contacts.
                        </label>
                    </li>
                </ul>
            </p>
        </div>
    </li>
</ul>