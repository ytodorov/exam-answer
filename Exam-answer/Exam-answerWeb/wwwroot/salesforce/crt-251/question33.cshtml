<ul class="panelbar">
    <li class="k-state-active">
        <span class="k-link k-state-selected">Question @Model?.Id</span>
        <div>
            <p>
                @Html.Raw(Model.H1OpenTag)
                Universal Containers has a customer base of over 15,000 Accounts and 60,000 Contacts.
                @Html.Raw(Model.H1CloseTag)
            </p>
            <p>
                @Html.Raw(Model.H2OpenTag)
                The marketing manager wants to use the customer data for an upcoming new product launch but is concerned contacts may have moved to different companies.
                @Html.Raw(Model.H2CloseTag)
            </p>
            <p>
                What should a consultant recommend to ensure customer data is accurate?
            </p>
        </div>
    </li>
    <li class="k-state-active">
        Answers
        <div>
            <p>
                <ul class="fieldlist">
                    <li class="correct">
                        <input type="radio" name="group" id="@(Html.GetNextId())" class="k-radio">
                        <label class="k-radio-label" for="@(Html.GetNextId(true))">
                            Use a data cleansing tool and the Stay-in-Touch feature of Salesforce to email contacts.
                        </label>
                    </li>
                    <li>
                        <input type="radio" name="group" id="@(Html.GetNextId())" class="k-radio">
                        <label class="k-radio-label" for="@(Html.GetNextId(true))">
                            Create a workflow rule for an account and contact owner to confirm contact data.
                        </label>
                    </li>
                    <li>
                        <input type="radio" name="group" id="@(Html.GetNextId())" class="k-radio">
                        <label class="k-radio-label" for="@(Html.GetNextId(true))">
                            Create a workflow rule to mass email the contacts and capture any email bounces.
                        </label>
                    </li>
                    <li>
                        <input type="radio" name="group" id="@(Html.GetNextId())" class="k-radio">
                        <label class="k-radio-label" for="@(Html.GetNextId(true))">
                            Use a data enrichment tool to verify account and contact data is up-to-date.
                        </label>
                    </li>
                </ul>
            </p>
        </div>
    </li>
</ul>