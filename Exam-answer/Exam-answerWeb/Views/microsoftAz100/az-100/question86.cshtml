<ul class="panelbar">
    <li class="k-state-active">
        <span class="k-link k-state-selected">Question @Model?.Id</span>
        <div>
            <p>
                @Html.Raw(Model.H1OpenTag)
                You have an Azure resource group named RG1. RG1 contains nine Windows Server 2016 virtual machines (VMs).
                @Html.Raw(Model.H1CloseTag)
            </p>
            <p>
                @Html.Raw(Model.H2OpenTag)
                You configure Azure to back up all VMs to a Recovery Services vault.
                @Html.Raw(Model.H2CloseTag)
            </p>
            <p>
                You need to view the most recent in-progress backup job data.
            </p>
            <p>
                How should you complete the Azure PowerShell command? To answer, select the appropriate options from the drop-down menus.
            </p>
        </div>
    </li>
    <li class="k-state-active">
        Answers
        TO DO
    </li>
    <li>
        Explanation
        <div>
            <p>
                You should use the following command:
            </p>
            <p>
                $joblist = Get-AzureRmRecoveryservicesBackupJob -Status "InProgress"
                <br />
                $joblist[0]
            </p>
            <p>
                To view the most recent in-progress backup job data, you should start with the Get-AzureRmRecoveryservicesBackupJob cmdlet, including the -Status parameter value "InProgress."
            </p>
            <p>
                You can then access the most recent entry in the backup job list by accessing the index value 0 in the $joblist array. You should not access $joblist because this returns the entire array. You should not access $joblist[1] because this operation does not retrieve the most recent backup job data.
            </p>
            <p>
                You should not use the Get-AzureRmBackupVault cmdlet because the focus of this scenario is individual backup job details, not the vault itself.
            </p>
            <p>
                You should not use the Get-AzureRmBackupItem cmdlet because it retrieves individual backed-up items from the Recovery Services vault, not backup job metadata.
            </p>
        </div>
    </li>
    <li>
        References
        <div>
            <p>
                <a href="https://docs.microsoft.com/en-us/azure/backup/backup-azure-vms-automation" target="_blank" rel="noopener">Use PowerShell to back up and restore virtual machines</a>
            </p>
            <p>
                <a href="https://docs.microsoft.com/en-us/powershell/module/azurerm.recoveryservices.backup/get-azurermrecoveryservicesbackupjob" target="_blank" rel="noopener">Get-AzureRmRecoveryServicesBackupJob</a>
            </p>
            <p>
                <a href="https://docs.microsoft.com/en-us/powershell/module/azurerm.backup/get-azurermbackupvault" target="_blank" rel="noopener">Get-AzureRmBackupVault</a>
            </p>
            <p>
                <a href="https://docs.microsoft.com/en-us/powershell/module/azurerm.backup/get-azurermbackupitem" target="_blank" rel="noopener">Get-AzureRmBackupItem</a>
            </p>
        </div>
    </li>
</ul>