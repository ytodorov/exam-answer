<ul class="panelbar">
    <li class="k-state-active">
        <span class="k-link k-state-selected">Question @Model?.Id</span>
        <div>
            <p>
                @Html.Raw(Model.H1OpenTag)
                What should a consultant recommend to meet this requirement?
                @Html.Raw(Model.H1CloseTag)
            </p>
            <p>
                @Html.Raw(Model.H2OpenTag)
                Universal Containers has a private sharing model for accounts and opportunities.
                @Html.Raw(Model.H2CloseTag)
            </p>
            <p>
                Each sales representative is assigned to work a dedicated sales engineer.
            </p>
            <p>
                The sales engineer will need access to their assigned sales representatives' accounts and opportunities.
            </p>
        </div>
    </li>
    <li class="k-state-active">
        Answers
        <div>
            <p>
                <ul class="fieldlist">
                    <li>
                        <input type="radio" name="group" id="@(Html.GetNextId())" class="k-radio">
                        <label class="k-radio-label" for="@(Html.GetNextId(true))">
                            Create a trigger to add the sales engineers to their sales representatives account and opportunity teams.
                        </label>
                    </li>
                    <li>
                        <input type="radio" name="group" id="@(Html.GetNextId())" class="k-radio">
                        <label class="k-radio-label" for="@(Html.GetNextId(true))">
                            Create criteria-based sharing rules to share the accounts and opportunities with sales engineers.
                        </label>
                    </li>
                    <li class="correct">
                        <input type="radio" name="group" id="@(Html.GetNextId())" class="k-radio">
                        <label class="k-radio-label" for="@(Html.GetNextId(true))">
                            Enable account and opportunity teams selling and have each sales representative configure their default teams.
                        </label>
                    </li>
                    <li>
                        <input type="radio" name="group" id="@(Html.GetNextId())" class="k-radio">
                        <label class="k-radio-label" for="@(Html.GetNextId(true))">
                            Have the sales representatives manually share the accounts and opportunities with their assigned sales engineers.
                        </label>
                    </li>
                </ul>
            </p>
        </div>
    </li>
</ul>