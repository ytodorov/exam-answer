<ul class="panelbar">
    <li class="k-state-active">
        <span class="k-link k-state-selected">Question @Model?.Id</span>
        <div>
            <p>
                @Html.Raw(Model.H1OpenTag)
                Universal containers sales team would like to track product shipments for each of its customers.
                @Html.Raw(Model.H1CloseTag)
            </p>
            <p>
                @Html.Raw(Model.H2OpenTag)
                The shipment tracking information is currently available in a back end system, which the company plans to integrate with Salesforce.
                @Html.Raw(Model.H2CloseTag)
            </p>
            <p>
                Which objects are relevant for this integration?
            </p>
        </div>
    </li>
    <li class="k-state-active">
        Answers
        <div>
            <p>
                <ul class="fieldlist">
                    <li>
                        <input type="radio" name="group" id="@(Html.GetNextId())" class="k-radio">
                        <label class="k-radio-label" for="@(Html.GetNextId(true))">
                            Opportunity, Opportunity product, Contract, Custom object –Shipment Status.
                        </label>
                    </li>
                    <li>
                        <input type="radio" name="group" id="@(Html.GetNextId())" class="k-radio">
                        <label class="k-radio-label" for="@(Html.GetNextId(true))">
                            Lead, Account, Opportunity Product, Custom object –Shipment Status.
                        </label>
                    </li>
                    <li class="correct">
                        <input type="radio" name="group" id="@(Html.GetNextId())" class="k-radio">
                        <label class="k-radio-label" for="@(Html.GetNextId(true))">
                            Opportunity, Opportunity product, Custom object –Shipment Status.
                        </label>
                    </li>
                    <li>
                        <input type="radio" name="group" id="@(Html.GetNextId())" class="k-radio">
                        <label class="k-radio-label" for="@(Html.GetNextId(true))">
                            Lead, Opportunity, Product, Custom object –Shipment Status.
                        </label>
                    </li>
                </ul>
            </p>
        </div>
    </li>
</ul>