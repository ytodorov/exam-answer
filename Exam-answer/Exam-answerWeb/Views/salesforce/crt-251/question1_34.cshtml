<ul class="panelbar">
    <li class="k-state-active">
        <span class="k-link k-state-selected">Question @Model?.Id</span>
        <div>
            <p>
                @Html.Raw(Model.H1OpenTag)
                Universal Containers acquires sales leads each year through trade shows. 
                @Html.Raw(Model.H1CloseTag)
            </p>
            <p>
                @Html.Raw(Model.H2OpenTag)
                Occasionally, duplicate leads are generated when the marketing team imports leads that already exist in the system.    
                @Html.Raw(Model.H2CloseTag)
            </p>
            <p>
                What should a consultant recommend to prevent duplicate leads in the system?
            </p>
        </div>
    </li>
    <li class="k-state-active">
        Answers
        <div>
            <p>
                <ul class="fieldlist">
                    <li>
                        <input type="radio" name="group" id="@(Html.GetNextId())" class="k-radio">
                        <label class="k-radio-label" for="@(Html.GetNextId(true))">
                            Upload the leads to Data.com to remove the duplicates and select the option to have them automatically imported.
                        </label>
                    </li>
                    <li>
                        <input type="radio" name="group" id="@(Html.GetNextId())" class="k-radio">
                        <label class="k-radio-label" for="@(Html.GetNextId(true))">
                            Upload the leads and click the “Find Duplicates” button for each of the leads to identify potential duplicate lead records.
                        </label>
                    </li>
                    <li>
                        <input type="radio" name="group" id="@(Html.GetNextId())" class="k-radio">
                        <label class="k-radio-label" for="@(Html.GetNextId(true))">
                            Upload the leads using Data Loader and enable the “Find Duplicates” setting to prevent duplicate records.
                        </label>
                    </li>
                    <li class="correct">
                        <input type="radio" name="group" id="@(Html.GetNextId())" class="k-radio">
                        <label class="k-radio-label" for="@(Html.GetNextId(true))">
                            Upload the leads using Data Import Wizard and select the appropriate field to match duplicates against existing records.
                        </label>
                    </li>
                </ul>
            </p>
        </div>
    </li>
</ul>