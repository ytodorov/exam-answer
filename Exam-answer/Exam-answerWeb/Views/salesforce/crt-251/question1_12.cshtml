<ul class="panelbar">
    <li class="k-state-active">
        <span class="k-link k-state-selected">Question @Model?.Id</span>
        <div>
            <p>
                @Html.Raw(Model.H1OpenTag)
                Universal Containers sells products that require frequent collaboration with the same team of individuals who play a key role in closing deals.
                @Html.Raw(Model.H1CloseTag)
            </p>
            <p>
                @Html.Raw(Model.H2OpenTag)
                The lead sales representative determines the level of access for each of the collaborating team members on an opportunity.
                @Html.Raw(Model.H2CloseTag)
            </p>
            <p>
                Which solution should a consultant recommend to facilitate the collaboration of the lead sales representative and team members?
            </p>
        </div>
    </li>
    <li class="k-state-active">
        Answers
        <div>
            <p>
                <ul class="fieldlist">
                    <li>
                        <input type="radio" name="group" id="@(Html.GetNextId())" class="k-radio">
                        <label class="k-radio-label" for="@(Html.GetNextId(true))">
                            Create public groups for extended team members and allow the sales representative to assign manual sharing on their opportunities.
                        </label>
                    </li>
                    <li>
                        <input type="radio" name="group" id="@(Html.GetNextId())" class="k-radio">
                        <label class="k-radio-label" for="@(Html.GetNextId(true))">
                            Define a sharing rule for each lead sales representative to assign appropriate access for all extended team members.
                        </label>
                    </li>
                    <li class="correct">
                        <input type="radio" name="group" id="@(Html.GetNextId())" class="k-radio">
                        <label class="k-radio-label" for="@(Html.GetNextId(true))">
                            Configure default opportunity teams for all lead sales representatives with team selling enabled.
                        </label>
                    </li>
                    <li>
                        <input type="radio" name="group" id="@(Html.GetNextId())" class="k-radio">
                        <label class="k-radio-label" for="@(Html.GetNextId(true))">
                            Enable Chatter to have the lead sales representative facilitate collaboration through sales team swarming.
                        </label>
                    </li>
                </ul>
            </p>
        </div>
    </li>
</ul>